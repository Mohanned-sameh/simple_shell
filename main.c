#include "main.h"
/**
 * main - Entry point
 * Return: 0
 */
int main(void)
{
	char *usetext, *textcp, *delimiter = " \n";
	char *strtoken, **av;
	int i = 0, numoftokens = 0;
	size_t num = 0;
	ssize_t numchars;

	printf("$ ");
	numchars = getline();
	return (0);
}
